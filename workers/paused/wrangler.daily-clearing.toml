# Paused deployment config for daily-clearing
# Requires DO delete-class migration to replace the old worker
name = "daily-clearing"
main = "src/index.ts"
compatibility_date = "2025-12-01"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["DigestJob", "UserState"]

[[migrations]]
tag = "v2"
deleted_classes = ["DigestJob", "UserState"]
