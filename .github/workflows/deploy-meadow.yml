name: Deploy Meadow

on:
  push:
    branches: [main]
    paths:
      - "apps/meadow/**"
      - "libs/engine/**"
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-pages.yml
    with:
      app-dir: apps/meadow
      project-name: grove-meadow
      needs-engine: true
    secrets: inherit
