name: Deploy Vista Collector

on:
  push:
    branches: [main]
    paths:
      - "workers/vista-collector/**"
      - "libs/engine/**"
      - "libs/gossamer/**"
      - "libs/infra/**"
      - "libs/prism/**"
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-worker.yml
    with:
      worker-dir: workers/vista-collector
      needs-engine: true
    secrets: inherit
