name: Deploy Loft

on:
  push:
    branches: [main]
    paths:
      - "workers/loft/**"
      - "libs/engine/**"
      - "libs/gossamer/**"
      - "libs/infra/**"
      - "libs/prism/**"
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-worker.yml
    with:
      worker-dir: workers/loft
      needs-engine: true
      run-tests: true
    secrets: inherit
