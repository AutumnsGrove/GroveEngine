/**
 * Curio Widget Components (Core 2)
 *
 * Only guestbook and poll survive as embeddable directives.
 * Gallery and Timeline are standalone features (not directive-mounted).
 */

export { default as CurioGuestbook } from "./CurioGuestbook.svelte";
export { default as CurioPoll } from "./CurioPoll.svelte";
